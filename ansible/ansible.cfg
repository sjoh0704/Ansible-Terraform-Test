[defaults]
inventory =  ./inventory/hosts
remote_user = centos
private_key_file = ../kubespray.pem
host_key_checking = False


[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false